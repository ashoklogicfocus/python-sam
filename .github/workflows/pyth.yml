name: python
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - uses: liquibase-github-actions/rollback-one-changest@v4
        with: 
          changelogfile: ""
          url: "" 
          headless: true
          licensekey: ${{ secrets. LIQUIBASE_LICENSE_KEY }}
          loglevel: INFO
